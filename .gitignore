node_modules/
.env
.env.local

# Store con datos personales (chat IDs + productos). NUNCA committear.
prices*.json
!prices.json.example

# Perfil persistente del scraper (cookies + localStorage de Amazon).
.browser-profile/

*.log
.DS_Store
Thumbs.db
